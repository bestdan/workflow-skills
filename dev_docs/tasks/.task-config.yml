handler: linear
linear:
  team: PreThink
  # 1Password op:// REFERENCE (never a raw key) to a Linear personal API key.
  # Powers the /archive-tasks GraphQL backstop and the /do-tasks read fast-path
  # (commands/handlers/assets/linear-ready.py). Resolved to the environment at
  # runtime — export LINEAR_API_KEY=$(op read "<this ref>"), or set
  # $OP_SERVICE_ACCOUNT_TOKEN for headless. Optional; unset → the MCP floor.
  api_key_ref: op://Private/cfzv6uerr27rk5ntvysuht5nxu/dan_local_key
  default_project: ebbc284b-a1c1-4cb3-96e0-914e210a79a2 # workflow-skills: Handler parity follow-ups
