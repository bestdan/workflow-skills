handler: gh-issue
gh-issue:
  repo: bestdan/workflow-skills
  # Task-loop marker. It matches the marker the transferred issues already carry,
  # and /archive-tasks + /reconcile-tasks refuse to sweep when it is unset.
  labels: [task-add]
  assignees: []
