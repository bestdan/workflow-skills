---
title: "research-spike: init — scaffold a project and add tracks"
priority: high
size: 3
status: new
created: 2026-08-01
source_branch: claude/spike-research-plan-jz7ggg
related_files:
  - dev_docs/designs/research_spike_skill.md
  - scripts/research-spike.py
  - scripts/test-research-spike.sh
is_blocked_by: research_spike_task_1
parent: research_spike
expires: 2026-08-31
tags: [research-spike, script]
---

[[research_spike_plan]]

## Context

`init` is what makes the reference implementation's three per-repo config knobs
unnecessary: the skill **owns** the directory convention, the coverage-file
location, and the `### Q<n>.` heading convention, so they are fixed by `init`
rather than configured per repo (design §"What is repo-specific", final
paragraph).

Scaffolding is the one place the script legitimately creates files. Everywhere
else, creating a file to make a record resolve is explicitly forbidden (design
§"What the skill must **not** do").

## Task

Implement `init` in `scripts/research-spike.py`:

- `init <project>` creates `<root>/dev_docs/research/<project>/` with:
  - `PROJECT.md` — charter stub: what is being built, why the spike exists, and
    a tracks index (empty at init).
  - `decisions.md` — empty, with a one-line header explaining that it is
    **organizer-owned** and that track agents file `state: proposed` blocks in
    their own `questions.md` instead.
  - `LEDGER.md` — the generated roll-up, with the ledger markers in place **and
    the zeroed roll-up lines the derivation produces for an empty project**, and
    a header stating it is generated by `write-ledger` and never hand-edited.
- `init <project> --track <name>` creates `tracks/<name>/` with:
  - `questions.md` — carrying the stored-ledger markers **and the zeroed ledger
    lines an empty track derives** (byte-identical to what `write-ledger` would
    emit, so the fresh tree is ledger-**fresh**, not merely marker-bearing — task
    7 makes a stale stored track ledger an error), plus a worked example of the
    `### Q<n>.` heading convention **inside an HTML comment** (`<!-- … -->`),
    showing the heading, its `question` block and an obligation declaration.
    The example must be **inert**: task 4's section discovery and coverage rule
    must not pick it up, or `init` emits a tree that fails its own gate. Inert
    but present is what installs the convention rather than documenting it.
  - `obligations/` (with a `.gitkeep`, since an empty dir does not survive git);
  - **no** `contracts/` — it is optional, and creating it eagerly would create
    an empty directory subject to the task-4 coverage rule.
- **Project and track names must match the same kebab-case shape as ids**
  (`^[a-z0-9]+(-[a-z0-9]+)*$`). This is grammar consistency, not security
  hardening: the names become the `project/track/` id-qualification prefix
  (task 1), so anything containing `/`, whitespace or `..` corrupts
  qualification, uniqueness reporting and task 3's cross-project destination
  check — all of which parse on that prefix. It also stops `init ../../outside`
  scaffolding outside the tree. Reject **before any filesystem operation** and
  exit `2` (a caller error — see task 1's exit-code classification).
- **Bare** `init <project>` on an existing project **fails** rather than
  overwriting; `init <project> --track <name>` naming an existing **track**
  likewise. Exit `2` with a message naming the existing path. (Only the bare
  form is forbidden on an existing project — the `--track` form is how a project
  grows, per the next bullet.)
- `--track` may be passed on the initial `init` and repeatedly afterwards
  (`init <project> --track <name>` on an existing project adds the track and is
  the documented way to grow one).
- Every generated markdown file must be `dprint`-stable — generated content
  that the formatter rewrites causes the same endless fight as a generated
  ledger (see task 7). Assert this in the tests below, not just by eye.
- The freshly-initialized tree must **pass `validate` clean** (exit 0). An
  `init` that produces a tree failing its own gate is the worst possible first
  impression; wire the assertion in tests now even though most rules land in
  tasks 3–5, and keep it passing as those land.

## Acceptance Criteria

**Code-enforced:**

- New fixtures in `scripts/test-research-spike.sh`:
  - `init foo` creates the four paths above and exits 0;
  - bare `init foo` twice fails (exit `2`) with a message naming the existing
    project, while `init foo --track bar` on that same existing project
    **succeeds**;
  - `init foo --track bar` creates `questions.md` with ledger markers and
    `obligations/.gitkeep`, and does **not** create `contracts/`;
  - a second `--track baz` on the same project succeeds and leaves `bar`
    untouched; a repeat `--track bar` fails (exit `2`);
  - `init ../outside`, `init /abs/path`, `init 'foo/bar'` and `--track '..'`
    each exit `2` with **no directory created** anywhere;
  - `validate` on the freshly-initialized tree exits `0`;
  - `init foo --track bar` immediately followed by `write-ledger` produces
    **no diff** — the fresh tree is ledger-fresh, not merely marker-bearing;
  - the scaffolded `### Q<n>.` example does **not** register as a question
    section: `validate` is clean and `status` reports zero questions;
  - `dprint fmt` over a copy of the generated files produces **no diff**
    (skip with a clear message if `dprint` is not on `PATH`, so the harness
    stays runnable on a bare machine).
- `bash scripts/check.sh` green.

**User-run:**

- `python3 scripts/research-spike.py --root /tmp/rs init demo --track account`
  produces a tree that reads as a usable starting point, not a stub dump —
  open `PROJECT.md` and `questions.md` and confirm a newcomer could file a
  question without reading the design doc.
