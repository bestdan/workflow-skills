<!-- Why is this change needed? Keep it about intent, not a diff summary. -->

## What & why

## Checklist

- [ ] `just check` passes locally
- [ ] If components changed: updated the README "N skills, M commands, K subagent" count
- [ ] If a skill changed/added: added or updated its `evals/prompts/*.txt` + `manifest.tsv` row
- [ ] If the version changed: bumped `plugin.json` and `marketplace.json` together
